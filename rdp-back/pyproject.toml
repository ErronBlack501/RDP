[project]
name = "rdp-back"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[project.scripts]
start = "uvicorn main:app --reload --host 0.0.0.0 --port 8000"

[tool.fastapi]
entrypoint = "main:app"
